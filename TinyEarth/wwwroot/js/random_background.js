/*var backImages = [
    "images/background/background_one.png",
]

function pageLoad() {
    document.getElementById("home_main").style.backgroundImage = 'url("' + encodeURI(backImages[Math.floor(Math.random() * (backImages.length))]) + '")';
}

function openLink(url) {
    window.location.href = url;
}

function openLinkNewTab(url) {
    window.open(url);
}